<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- No READ_SMS / RECEIVE_SMS: SMS ingestion is via the user-initiated Share intent only. -->

    <application
        android:allowBackup="false"
        android:label="sohojAI"
        android:supportsRtl="true"
        android:theme="@style/Theme.SohojAI">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Receive shared bKash SMS text from the Messaging app -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Reads ONLY bKash notifications; everything else is ignored and never stored. -->
        <service
            android:name=".ingest.BkashNotificationListenerService"
            android:exported="true"
            android:label="sohojAI bKash detection"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
