// Standalone Kotlin/JVM module for the deterministic bKash parser.
// Built/tested without the Android SDK; wired into the Android app as
// :core-parser during the Android scaffolding step.
rootProject.name = "sohojai-parser"
